<%= render 'layouts/header' %>

<div class="bg">
<div class="container">
<%= render 'layouts/loginstatus' %>


<div class="card card-body text-center" style="width: 18rem;">
  <div class="card-body">
  	<h5 class="card-title"><%= @smoothie.smoothie_name %></h5>
  	<% if @smoothie.image.attached? %>
  		<%= image_tag @smoothie.image %>
  	<% else %>
  		<%= image_tag 'no_image.jpg' %>
  	<% end %>
  	<p class="card-text my-2"><%= @smoothie.comment %></p>
  	<div class="mx-auto text-center">
		<%= link_to "編集する", edit_smoothy_path ,class: "btn btn-warning" %>

		<!-- お気に入り -->
		<% if @smoothie.favorited_by?(current_user) %>
			<%= link_to "お気に入り解除",smoothy_favorite_path(@smoothie), method: :delete ,class: "btn btn-warning mt-2" %>
	  	<% else %>
	  		<%= link_to "お気に入り登録",smoothy_favorite_path(@smoothie), method: :post ,class: "btn btn-warning mt-2" %>
	  	<% end %>
	</div>
	<p class="text-right card-date favorite">
	  		<!-- 日付 -->
	  		<%= @smoothie.created_at.strftime('%Y.%m.%d')  %>
	  		<!-- お気に入り数 -->
	  	  <% if @smoothie.favorited_by?(current_user) %>
	  		<%= link_to smoothy_favorite_path(@smoothie), method: :delete do %>
		      <%= image_tag 'heart_off.png', :size =>'14x14' %>
		      <%= @smoothie.favorites.count %>
		    <% end %>
	  	  <% else %>
	  		<%= link_to smoothy_favorite_path(@smoothie), method: :post do %>
		      <%= image_tag 'heart_on.png', :size =>'14x14' %>
		      <%= @smoothie.favorites.count %>
		    <% end %>
	  	  <% end %>
	  	</p>
  </div><!-- card-body -->
</div><!-- card -->

<p>栄養成分チャート</p>
<p>β-カロテン:</p>
<p>カルシウム:</p>
<p>カリウム:</p>
<p>マグネシウム:</p>
<p>ビタミンC:</p>
<p></p>
<p>kcal:</p>

</div><!-- container -->
</div><!-- bg -->